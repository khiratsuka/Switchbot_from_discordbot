name: Deploy to GCP-VM
 
on:
  push:
    branches:
      - feature-actions
 
jobs:
  deploy:
    permissions:
      contents: 'read'
      id-token: 'write'

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
 
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          project_id: switchbot-from-discordbot
          workload_identity_provider: ${{ secrets.IDENTITY_PROVIDER }}
          service_account: ${{ secrets.SERVICE_ACCOUNT }}

      - name: test on VM
        run: |
          gcloud config configurations list
      #gcloud compute ssh khiratsuka-containers@khiratsuka-containers-vm \
      #  --quiet \
      #  --command='
      #    echo "Hello from GCP VM" >> ~/test.txt
      #  '
      #- name: SSH and Pull Repo in GCP VM
      #  id: git_pull
      #  run: |
      #echo "::set-output name=git_pull_output::$(
      #    gcloud compute ssh user@example-instance \
      #      --zone asia-northeast1-b \
      #      --quiet \
      #      --command='
      #        cd path/to/repository && 
      #        git pull
      #      '
      #  )"